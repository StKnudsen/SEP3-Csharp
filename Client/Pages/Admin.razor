@page "/Admin"
@using Client.Connection.Administration
@inject IAdministration _administration
<h3>Admin</h3>

<div>Tilføj ingredienser</div>
<input type="text" @bind-value="ingredientName"/>
<!-- Der skal laves noget foreach med fødevaregrupper fra databasen, og dem der er markeret
i checkboxene skal sendes med-->
<select name="_foodgroupId">
    @{
        foreach (KeyValuePair<int, string> pair in FoodGroupList)
        {
            <option value="@pair.Key" @bind-value="_foodGroupId">@pair.Value</option>
        }
    }
</select>

<input type="submit" @onclick="AddIngredientAsync"/>

@code {
    public int _foodGroupId;
    public string ingredientName { get; set; }

    public Dictionary<int, string> FoodGroupList;

    protected override async Task OnInitializedAsync()
    {
     FoodGroupList =  await _administration.getFoodgroupsListAsync();
    }
    
    private async Task AddIngredientAsync()
    {
        await _administration.AddIngredientAsync(ingredientName, _foodGroupId);
    }

}