@page "/Groups/{groupId}"
@using SharedLibrary.Models
@using Client.Connection.GroupManagement
@inject NavigationManager NavigationManager
@inject IGroupManager GroupManager

@attribute [Authorize(Policy = "SignedIn")]

<AuthorizeView>
    @if (group == null)
    {
        <p>Page Loading...</p>
    }
    else
    {
        <div class="mt-6 p-6 max-w-sm mx-auto bg-white rounded-xl shadow-md flex items-center space-x-4 border border-gray-300 flex flex-wrap">
            <form class="w-full max-w-lg">
                <div class="flex flex-wrap mx-3 mb-6 justify-evenly">
                    <div class="w-full md:w-1/2 px-3 mb-6 md:mb-0">
                        <label class="block uppercase tracking-wide text-gray-700 text-xs font-bold mb-2" for="grid-recipes">
                            Opskrifter
                        </label>
                        <input class="border rounded border-gray-400 mb-2" id="grid-recipes" type="radio" name="toggle" @bind-value="recipes">
                    </div>
                    <div class="w-full md:w-1/2 px-3">
                        <label class="block uppercase tracking-wide text-gray-700 text-xs font-bold mb-2" for="grid-restaurants">
                            Restauranter
                        </label>
                        <input class="border rounded border-gray-400 mb-2" id="grid-restaurants" type="radio" name="toggle" @bind-value="restaurants">
                    </div>
                </div>
                <div class="flex flex-wrap mx-3 mb-6 justify-evenly">
                    <div class="w-full md:w-1/2 px-3 mb-6 md:mb-0">
                        <label class="block uppercase tracking-wide text-gray-700 text-xs font-bold mb-2" for="grid-zip">
                            Postnummer
                        </label>
                        <input class="w-full bg-gray-200 text-gray-700 border border-gray-200 rounded py-3 px-4" id="grid-zip" type="text" placeholder="8700" @bind-value="zip">
                    </div>
                    <div class="w-full md:w-1/2 px-3">
                        <label class="block uppercase tracking-wide text-gray-700 text-xs font-bold mb-2" for="grid-start">
                         </label>
                        <input class="mt-4 bg-red-800 hover:bg-red-600 duration-500 transition text-gray-100 font-semibold py-3 px-8 border border-gray-400 rounded shadow" id="grid-start" type="button" value="Start" @onclick="StartGroupSwipe">
                    </div>
                </div>
            </form>
            <table class="w-full divide-gray-200 border border-gray-200 shadow rounded overflow-auto">
                <thead class="bg-gray-200">
                <th class="block uppercase tracking-wide text-gray-700 text-xs font-bold mb-2">Gruppemedlemmer</th>
                </thead>
                <tbody class="bg-white divide-gray-200">
                <div class="text-sm text-gray-500">
                    <tr>
                        <td>Et eller andet</td>
                    </tr>
                </div>
                </tbody>
            </table>
        </div>
    }
</AuthorizeView>


@code {

    [Parameter]
    private string groupId { get; set; }
    private Group group { get; set; }
    
    private bool recipes { get; set; }
    private bool restaurants { get; set; }
    private int? zip { get; set; }

    protected override async Task OnInitializedAsync()
    {
        group = await GroupManager.GetGroupFromIdAsync(groupId);
    }


    private void StartGroupSwipe()
    {
        if (recipes)
        {
            //todo: noget
        }
        else if(restaurants)
        {
            //todo: noget andet med zip
        }
    }
}